version: '3.8'

services:
  rag-assistant:
    build: .
    container_name: rag-sales-assistant
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    restart: unless-stopped